# Backlog: noticed along the way

<!-- Form only: one line per entry, no statuses/priorities/owners. Behavior (when a line is written, when
     promoted) lives in the playbooks — on conflict the playbook wins. These comments are not copied anywhere.

     A sink, not a tracker. Written by ANY skill's foreign-finding rule (task, debug, craft-graph, …): noticed
     outside the current work's scope → a line here → keep going.
     Triage by a human: a line became a brief/task → delete it; no longer relevant → delete it deliberately.
     One machine-triaged kind: a `graph-candidate` line (a durable lesson, `file:line` proof inside) — the
     nearest M/L wrap or a craft-graph pass promotes it into docs/graph/ and replaces the line with a pointer
     `→ [[slug]]` (consumed, not deleted — so it can't re-promote into a duplicate).
     The file lives in docs/crafts/_backlog.md — the "_" keeps it above the task folders. -->

- YYYY-MM-DD [task-slug or chat]: the gist in one line
- YYYY-MM-DD [task-slug]: graph-candidate — the durable lesson in one line (path/file.py:120)
- YYYY-MM-DD [task-slug]: graph-candidate → [[slug]] (promoted)
